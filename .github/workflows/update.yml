name: Update server production
run-name: ${{ github.actor }} push in Main branch 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: chmod ${{ github.workspace }}/update.sh
      - run: ${{ github.workspace }}/update.sh
